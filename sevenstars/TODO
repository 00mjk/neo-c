メソッドの上書き、メソッドの構造体にparent methodへのポインタを入れる。inheritで呼び出す

親クラスのメソッド呼び出し。super

ファイルの書き込み。stringクラスにwrite,append

リフレクション。

--------------
vm return false message
!exp
inheritance
object equal
object type_name
string.libraries
string.set_value オブジェクトを持つオブジェクトにstringを変更
macro
normal block

classの保存
generics
map, tuple

load fundamental classes
vim("src/node.nc で実行
C-c
if(true) { 1.to[TAB]
while(true) { 1.[TAB]
if(1.[TAB]
while(1.[TAB]
if(true) { l[TAB]
readline
C-z, jobs, fg

