readline
vim("src/node.nc で実行

メソッドの上書き、メソッドの構造体にparent methodへのポインタを入れる。inheritで呼び出す

親クラスのメソッド呼び出し。super

--------------
vm return false message
!exp
inheritance
object equal
object type_name
string.libraries
string.set_value オブジェクトを持つオブジェクトにstringを変更
macro
normal block

classの保存
generics
map, tuple

