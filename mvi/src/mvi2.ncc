#include "neo-c.h"

extern "C"
{
#include <stdio.h>
#include <stdlib.h>
#include <ncurses.h>
}

#include "mvi.h"

struct mvi {
    b:int;
}

impl mvi version 2 {
    initialize() {
        inherit(self);
        puts("initialize mvi2");
    }

    finalize() {
        inherit(self);
        puts("finalize mvi2");
    }

    def main(self:mvi*):int {
        inherit(self);

        puts("finish app");

        0
    }
}
