-g option

int a = true; => a == -1


error number bug in if, impl

static local variable

alloc initialize
alloc destructor
machine stack structor called destructor when deleting
rm neo-c.h.out
Pointer minus Pointer valid size
Array casts Pointer
-g ivalid line number
if error line number bug
LLVM3.8 OPTIMIZE
LLVM7.0 -g
----------------------
, 演算子
__extention__ (exp)

sizeof(exp)

if() ; else ;

if(true) {
}
*v = が}のあとに;をつけないとだめ。パーサーのバグ
